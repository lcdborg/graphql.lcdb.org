import { FormatPerformanceDatePipe } from './format-performance-date.pipe';

describe('FormatPerformanceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPerformanceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
